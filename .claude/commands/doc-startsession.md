---
description: Initialize Session Context
---
1. Read `./docs/system-rules.md`. These are absolute constraints. You must strictly adhere to them for all code generation.
2. Read `./docs/architecture.md` to understand the structural boundaries and module design.
3. Read `./docs/active-context.md` to understand the current task, recent progress, and immediate goals.
4. Run `head -n 10` on any other `*.md` files in `docs/` to map the rest of the documentation.
5. Do not output summaries or greetings. Output only: `Context loaded. Ready to work on active tasks.`